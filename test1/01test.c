typedef struct ListNode {   // 단순 연결 리스트의 노드 구조 정의

    int data;
    listNode* link;
} listNode;

typedef struct {       // 리스트의 헤드(first) 노드 구조 정의

    listNode* head;

} linkedList_h;

int main() {
    return 0;
}